# A2A 协议 — tRPC-Agent-Go 实现深度

> **前置阅读**：[A2A 协议课程集](/a2a/)（7 篇文章，从零理解 A2A 协议规范）

A2A（Agent-to-Agent）是 Google 提出的跨框架 Agent 互操作标准。tRPC-Agent-Go 基于 tRPC-A2A-Go 实现了完整的 A2A 客户端，可以将任何兼容 A2A 的远程 Agent 作为本地 Agent 使用。

## 1. A2A 在 tRPC-Agent-Go 中的位置

```
┌──────────────────────────────────────────────────────────┐
│                  Coordinator (LLMAgent)                   │
│                                                          │
│  SubAgents:                                              │
│  ├── WeatherAgent (本地 LLMAgent)                        │
│  ├── MathAgent (本地 LLMAgent)                            │
│  ├── A2AAgent → Remote Python ADK Agent（A2A 协议）       │
│  └── A2AAgent → Remote LangChain Agent（A2A 协议）        │
│                                                          │
│  对 Coordinator 来说，所有 SubAgent 都是 agent.Agent 接口 │
│  无论本地还是远程，调用方式完全一致                         │
└──────────────────────────────────────────────────────────┘
```

## 2. A2A Agent 客户端

```go
import (
    "trpc.group/trpc-go/trpc-agent-go/agent/a2aagent"
    "trpc.group/trpc-go/trpc-a2a-go/client"
)

// 1. 创建 A2A 客户端
a2aClient, _ := client.New(
    client.WithBaseURL("https://remote-agent.example.com"),
    client.WithHTTPClient(&http.Client{Timeout: 60 * time.Second}),
)

// 2. 包装为 tRPC-Agent-Go Agent
remoteAgent := a2aagent.New(
    a2aagent.WithA2AClient(a2aClient),
    a2aagent.WithStreaming(true),
)

// 3. 当作本地 Agent 使用
coordinator := llmagent.New("coordinator",
    llmagent.WithModel(model),
    llmagent.WithSubAgents([]agent.Agent{
        localWeatherAgent,
        remoteAgent, // 与本地 Agent 并列
    }),
)

r := runner.NewRunner("app", coordinator)
events, _ := r.Run(ctx, "user-1", "session-1",
    model.NewUserMessage("Analyze the market data and give weather forecast."),
)
```

**关键设计**：`A2AAgent` 实现了 `agent.Agent` 接口，对上层完全透明。Coordinator 不关心 SubAgent 是本地的还是远程的。

## 3. A2A 任务生命周期

```
[Go Client]                              [Python Remote Agent]
     │                                            │
     ├─ POST /tasks ────────────────────────────→ │
     │   {method: "tasks/send",                    │
     │    params: {id: "task-1",                   │
     │            message: {role:"user",           │
     │                      parts:[{text:"..."}]}}}│
     │                                            ├─ Agent 处理
     │                                            ├─ 调用 LLM
     │                                            ├─ 执行工具
     │  ←────── SSE event: task status ────────   │
     │   event: status                             │
     │   data: {state: "working", ...}             │
     │                                            │
     │  ←────── SSE event: artifact ───────────   │
     │   event: artifact                           │
     │   data: {parts: [{text: "The analysis..."}]}│
     │                                            │
     │  ←────── SSE event: final ──────────────   │
     │   event: final                              │
     │   data: {state: "completed"}                │
```

**TaskStatus 状态机**：

```
                    ┌─────────────┐
                    │  submitted  │ (初始状态)
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │   working   │ (Agent 执行中)
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
     ┌────────▼───┐  ┌────▼─────┐  ┌───▼────────┐
     │  completed │  │  failed  │  │  cancelled  │
     └────────────┘  └──────────┘  └────────────┘
```

## 4. Agent Card 发现

A2A Agent 通过 Agent Card 声明自己的能力和接口：

```json
// GET https://remote-agent.example.com/.well-known/agent-card.json
{
    "name": "Weather Analyst",
    "description": "Analyzes weather patterns and provides forecasts",
    "url": "https://remote-agent.example.com",
    "capabilities": {
        "streaming": true,
        "pushNotifications": false
    },
    "skills": [
        {
            "id": "weather_analysis",
            "name": "Weather Analysis",
            "description": "Analyze historical weather data and provide forecasts",
            "tags": ["weather", "analysis", "forecast"],
            "examples": ["What's the weather forecast for Tokyo?"]
        }
    ],
    "defaultInputModes": ["text"],
    "defaultOutputModes": ["text"],
    "authentication": {
        "schemes": ["bearer"]
    }
}
```

tRPC-Agent-Go 可以基于 Agent Card 自动发现远程 Agent：

```go
// 自动发现模式：通过 Agent Card URL
agent, _ := a2aagent.New(
    a2aagent.WithAgentCardURL("https://remote-agent.example.com"),
)

// 注册中心发现模式
agent, _ := a2aagent.New(
    a2aagent.WithRegistryURL("https://registry.example.com"),
    a2aagent.WithAgentSelector("weather_analysis"), // 按 skill 选择
)
```

## 5. A2A 数据模型

### 5.1 Message

```json
{
    "role": "user",
    "parts": [
        {"type": "text", "text": "What's the weather in Tokyo?"}
    ],
    "metadata": {
        "timestamp": "2026-06-10T10:00:00Z",
        "user_locale": "zh-CN"
    }
}
```

### 5.2 Task

```json
{
    "id": "task-uuid-123",
    "sessionId": "session-456",
    "status": {
        "state": "working",
        "message": {"role": "agent", "parts": [{"type": "text", "text": "Analyzing..."}]},
        "timestamp": "2026-06-10T10:00:05Z"
    },
    "artifacts": [
        {
            "name": "forecast.json",
            "parts": [{"type": "text", "text": "{\"temperature\": 22, \"condition\": \"sunny\"}"}]
        }
    ],
    "history": [/* Message 列表 */]
}
```

### 5.3 Artifact

```json
{
    "name": "weather_chart.png",
    "parts": [
        {
            "type": "file",
            "file": {
                "url": "https://remote-agent.example.com/artifacts/chart-123.png",
                "mimeType": "image/png",
                "bytes": "base64_encoded_data..."
            }
        }
    ]
}
```

## 6. A2A vs AgentTool 选择指南

| | A2A Agent | AgentTool |
|----|----|----|
| **通信方式** | HTTP/SSE 跨进程 | 进程内函数调用 |
| **协议标准** | ✅ A2A 开放标准 | ❌ 框架专有 |
| **跨语言** | ✅ 任何语言 | ❌ 仅 Go |
| **跨框架** | ✅ 任何 A2A 实现 | ❌ 仅 tRPC-Agent-Go |
| **延迟** | 网络延迟 + 序列化 | 零开销函数调用 |
| **独立部署** | ✅ 独立扩缩 | ❌ 共享进程资源 |
| **安全隔离** | ✅ 进程/网络隔离 | ❌ 共享内存 |
| **状态管理** | A2A Session | Agent Invocation |
| **适用场景** | 跨团队/跨组织 Agent | 同一进程内的专家 Agent |

**选择决策树**：

```
需要跨团队/跨语言/跨框架协作？
├── 是 → A2A Agent
└── 否 → 同一进程内拆分 Agent？
         ├── 是 → AgentTool 或 SubAgents
         └── 否 → 单一 LLMAgent 即可
```

## 7. A2A + MCP 组合模式

```
[User Frontend]
     │ AG-UI (SSE)
     ▼
[Go Coordinator Agent]
     │
     ├── A2A ──→ [Python Data Analysis Agent]
     │               │
     │               ├── MCP ──→ [Database Tool Server]
     │               └── MCP ──→ [Chart Generation Server]
     │
     └── MCP ──→ [Internal API Server]
```

这是一个典型的"Go 做编排、Python 做分析"的混合架构：
- Go Coordinator 通过 A2A 将分析任务委托给 Python Agent
- Python Agent 通过 MCP 调用数据库和图表工具
- Go Coordinator 同时通过 MCP 调用内部 API
- 前端通过 AG-UI 实时看到整个协作过程
